# ansible_ec2_setup/tasks/setup_certbot.yml
- name: Certbot & Apache plugin install
  yum:
    name: certbot-apache       
    state: present

- name: Obtain/renew Let’s Encrypt cert
  command: >-
    certbot --apache -n --agree-tos
            -m {{ renzoku1875@gmail.com }}
            -d {{ mytest-portfolio.xyz }}
  args:
    creates: /etc/letsencrypt/live/{{ mytest-portfolio }}/fullchain.pem
  notify: reload httpd

